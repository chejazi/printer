# Required: bearer token for POST /print and GET /printers
AUTH_TOKEN=change-me-to-a-long-random-string

# HTTP port (default: 3000)
PORT=3000

# CUPS printer queue name (default: USB_80Series2)
PRINTER_NAME=receipt

# Batch queue: wait this long after the last message before printing (default: 150)
COALESCE_MS=150

# Batch queue: print at least this often during a steady trickle (default: 2000)
MAX_BATCH_WAIT_MS=2000

# Pause between completed CUPS batches (default: 500)
JOB_DELAY_MS=500

# Cancel a CUPS job if it has not finished after this many ms (default: 30000)
JOB_TIMEOUT_MS=30000

# Reject new messages when this many are waiting (default: 100)
MAX_QUEUE_MESSAGES=100
